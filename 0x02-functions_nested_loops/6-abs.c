#include "main.h"
/**
 *_abs - computes the total value of a integer
 *@n: the int to check
 *Return: the absolute valude of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
